# iPOS - التوثيق الكامل للتطبيق والبنية التقنية

يقدم هذا المستند نظرة شاملة ومتكاملة على تطبيق iPOS، بدءًا من وصف الميزات الوظيفية لكل صفحة، وصولًا إلى شرح تفصيلي للبنية التقنية والقرارات الهندسية التي تضمن أداء التطبيق وموثوقيته، مع التركيز على اعتماده الكامل على Dexie.js للعمل بدون اتصال بالإنترنت.

---

## 1. المفاهيم الأساسية ومقدمة

**iPOS** هو نظام نقاط بيع (POS) حديث وعالي الأداء، مصمم خصيصًا لمتاجر التجزئة التي تحتاج إلى حل مستقل وموثوق. إنه **تطبيق ويب تقدمي (PWA)** يعمل بالكامل بدون اتصال بالإنترنت، مما يضمن استمرارية العمل حتى في غياب اتصال ثابت بالشبكة.

### ⚠️ بياناتك تحت سيطرتك الكاملة

نقطة جوهرية في تصميم iPOS هي أن جميع بياناتك (المنتجات، المبيعات، العملاء، إلخ) تُخزن **حصريًا في قاعدة بيانات IndexedDB داخل متصفحك** على جهازك الحالي.

-   **لا سحابة، لا حسابات:** لا تتم مزامنة أي بيانات تلقائيًا مع خادم سحابي، ولا توجد حسابات مستخدمين. هذا يمنحك خصوصية تامة وسيطرة كاملة على معلوماتك.
-   **مسؤولية النسخ الاحتياطي:** بما أن البيانات محلية، فأنت المسؤول الوحيد عن حمايتها. يجب عليك استخدام ميزة **النسخ الاحتياطي والاستعادة** (`الإعدادات ← النسخ الاحتياطي`) بانتظام لتصدير بياناتك وحفظها في مكان آمن (مثل قرص صلب خارجي أو خدمة تخزين سحابي خاصة بك).

---

## 2. البنية التقنية العميقة

يقوم iPOS على حزمة تقنية حديثة تم اختيارها بعناية لتحقيق الأداء العالي، الموثوقية، وتجربة تطوير ممتازة.

### أ. حزمة التكنولوجيا (Tech Stack)

-   **إطار العمل الرئيسي:** [Next.js (App Router)](https://nextjs.org/) - يوفر بنية قوية قائمة على المكونات (Components)، ونظام توجيه (Routing) يعتمد على الملفات، وقدرات العرض من جانب الخادم (SSR) التي يتم التحكم فيها بدقة.
-   **تخزين البيانات:** **[Dexie.js](https://dexie.org/)** - هذه هي حجر الزاوية في بنية التطبيق. Dexie.js هي مكتبة قوية تعمل كطبقة تجريدية فوق IndexedDB، مما يسهل التعامل مع قاعدة البيانات المحلية. يتم تخزين جميع بيانات التطبيق في قاعدة بيانات منظمة على جهاز المستخدم، مما يضمن:
    -   **استمرارية قوية للبيانات:** تبقى البيانات متاحة حتى بعد إغلاق المتصفح أو إعادة تشغيل الجهاز.
    -   **استعلامات معقدة وعالية الأداء:** بفضل الفهارس (Indexes) المحددة، يمكن إجراء عمليات بحث وتصفية وفرز سريعة جدًا.
    -   **سلامة البيانات:** من خلال المعاملات الذرية (Atomic Transactions).
-   **طبقة الوصول إلى البيانات:**
    -   **`database.ts`**: هذا الملف هو قلب إدارة قاعدة البيانات. يُعرّف مخطط (Schema) قاعدة البيانات `posDB`، بما في ذلك جميع الجداول (`products`, `sales`, إلخ) والفهارس الخاصة بها. الأهم من ذلك، أنه يوفر دالة `getDb()` الآمنة للاستخدام من جانب الخادم (SSR-safe) للحصول على نسخة فريدة (Singleton) من قاعدة البيانات.
    -   **`data-service.ts`**: هذه الخدمة هي الواجهة الوحيدة التي يجب على باقي التطبيق استخدامها للتفاعل مع قاعدة البيانات. تحتوي على جميع دوال جلب البيانات وتعديلها (مثل `getProducts`, `addSale`). جميع عمليات الكتابة المعقدة يتم تنفيذها داخل معاملات `Dexie.transaction()` لضمان سلامة البيانات.
-   **إدارة الحالة وتحديثات واجهة المستخدم:**
    -   **[dexie-react-hooks](https://dexie.org/docs/dexie-react-hooks/useLiveQuery())**: يستخدم التطبيق الخطاف `useLiveQuery` على نطاق واسع. هذا الخطاف يربط مكونات React مباشرة باستعلامات قاعدة البيانات. أي تغيير في البيانات يؤدي تلقائيًا إلى إعادة تنفيذ الاستعلام وإعادة عرض المكونات التي تستخدمه، مما يضمن واجهة مستخدم محدثة دائمًا وبدون الحاجة إلى إدارة حالة يدوية معقدة.
-   **واجهة المستخدم (UI):**
    -   **[ShadCN UI](https://ui.shadcn.com/)**: مجموعة من مكونات واجهة المستخدم الجميلة، القابلة لإعادة الاستخدام، والمصممة بشكل احترافي.
    -   **[Tailwind CSS](https://tailwindcss.com/)**: إطار عمل CSS يركز على الأدوات المساعدة (utility-first) لتصميمات مخصصة وسريعة.
-   **PWA (Progressive Web App):** تم تصميم التطبيق ليعمل كتطبيق ويب تقدمي، مما يسمح بتثبيته على سطح المكتب أو الهاتف المحمول وتشغيله بنسبة 100% بدون اتصال بالإنترنت.

### ب. إدارة البيانات مع Dexie.js (نظرة تفصيلية)

1.  **`database.ts` - مصدر الحقيقة الوحيد للاتصال:**
    -   **التعريف والفهرسة:** هذا الملف يعرّف `PosDatabase` التي ترث من `Dexie`. يستخدم `this.version().stores()` لتحديد جميع الجداول وفهارسها. الفهارس ضرورية للغاية للأداء، على سبيل المثال `[category+name]` لجدول `products` يسمح بتصفية وفرز فعال للمنتجات حسب الفئة والاسم.
    -   **الترقيات (Upgrades):** يحتوي على منطق لترحيل البيانات بين إصدارات المخطط المختلفة، مما يضمن عدم فقدان بيانات المستخدم عند تحديث التطبيق.
    -   **دالة `getDb()` الآمنة:** توفر هذه الدالة وصولًا آمنًا إلى قاعدة البيانات. في بيئة الخادم (SSR)، حيث لا يوجد `IndexedDB`، تقوم الدالة بإلقاء خطأ واضح وصريح، مما يمنع أخطاء البناء والتشغيل الغامضة ويفرض على المطورين استخدامها فقط في مكونات العميل (`'use client'`).

2.  **`useLiveQuery` لقراءة البيانات:**
    -   تستخدم جميع مكونات التطبيق التي تحتاج إلى عرض بيانات الخطاف `useLiveQuery` من `dexie-react-hooks`.
    -   **مثال:** `const products = useLiveQuery(() => dataService.getProducts({ query }), [query]);`
    -   هذا الخطاف يراقب الجداول التي يستعلم عنها. عند حدوث أي تغيير (إضافة، تحديث، حذف) في تلك الجداول، يعيد `useLiveQuery` تشغيل الدالة المقدمة له تلقائيًا، مما يؤدي إلى تحديث البيانات في المكون وإعادة عرضه بواجهة مستخدم متزامنة مع قاعدة البيانات.

3.  **`data-service.ts` لكتابة البيانات ومنطق الأعمال:**
    -   للحفاظ على فصل واضح للمسؤوليات، يتم تركيز جميع عمليات الكتابة (`add`, `update`, `delete`) والمنطق المعقد في `data-service`.
    -   تستدعي المكونات هذه الدوال لتعديل البيانات (على سبيل المثال، `dataService.addProduct(newProduct)`).
    -   **المعاملات الذرية (Atomic Transactions):** جميع العمليات الحساسة التي تتضمن تحديث جداول متعددة (مثل إتمام عملية بيع) يتم تغليفها داخل `db.transaction()`. هذا يضمن أنه إما أن تنجح جميع العمليات معًا أو تفشل جميعها معًا، مما يمنع تمامًا أي تناقض في البيانات (على سبيل المثال، بيع منتج دون تقليل مخزونه).

---

## 3. شرح مفصل للصفحات والميزات

### أ. لوحة التحكم (`/dashboard`)

#### الوصف الوظيفي
هذه هي الصفحة الرئيسية والمركز العصبي للتطبيق، حيث توفر نظرة عامة في الوقت الفعلي على أداء العمل.

-   **الهدف:** توفير مؤشرات الأداء الرئيسية (KPIs) ونظرة سريعة على النشاط التجاري.
-   **الميزات:**
    -   **محدد الفترة الزمنية:** مرشح تاريخ قوي لتحليل البيانات عبر فترات مخصصة (اليوم، آخر 7 أيام، هذا الشهر، إلخ).
    -   **بطاقات الإحصائيات:** تعرض الأرقام الأساسية: إجمالي الإيرادات، صافي الربح، عدد المبيعات، إجمالي المصروفات، والقيمة الإجمالية للمخزون.
    -   **تنبيهات انخفاض المخزون:** تسلط الضوء على المنتجات التي وصل مخزونها إلى الحد الأدنى المحدد أو انخفض عنه.
    -   **مخطط الإيرادات والربح:** رسم بياني شريطي مرئي يقارن الإيرادات وصافي الأرباح يوميًا خلال الفترة المحددة.
    -   **أفضل المنتجات والعملاء:** قوائم تعرض المنتجات الأكثر مبيعًا والعملاء الأكثر إنفاقًا.
    -   **النشاط الأخير:** عرض لآخر 10 عمليات مسجلة في النظام (بيع، شراء، عميل جديد، إلخ).

#### التصميم التقني
-   **جلب البيانات:** تستخدم الصفحة الخطاف المخصص `useDashboardData` لجلب جميع بياناتها بشكل تفاعلي. هذا الخطاف بدوره يستخدم `useLiveQuery` لتجميع البيانات من جداول متعددة (`sales`, `products`, `expenses`) بناءً على النطاق الزمني المحدد. هذا يضمن تحديث لوحة التحكم في الوقت الفعلي عند حدوث أي عملية بيع أو إضافة منتج جديد.
-   **إدارة الحالة:** يتم التحكم في النطاق الزمني بواسطة الخطاف المخصص `useDateRange` الذي يدير حالة فترة العرض.
-   **العرض:** تستخدم مكونات من مكتبة `Recharts` لعرض المخططات البيانية، ومكونات واجهة مستخدم مخصصة (`KPICard`, `StockAlertsCard`, `RecentActivityCard`) لعرض الإحصائيات.

### ب. نقطة البيع (`/sell`)

#### الوصف الوظيفي
واجهة الكاشير الرئيسية، مصممة لتكون سريعة وبديهية وفعالة.

-   **الهدف:** تمكين تسجيل المبيعات السريع وإدارة المعاملات.
-   **الميزات:**
    -   **إدارة سلات متعددة:** إمكانية إدارة عدة سلات تسوق في وقت واحد عبر علامات تبويب.
    -   **بحث المنتجات:** حقل بحث قوي للعثور على المنتجات بالاسم أو مسحها ضوئيًا بالباركود.
    -   **منتج مخصص:** إمكانية إضافة منتج غير موجود في المخزون إلى السلة بسرعة.
    -   **اختيار العميل:** ربط عملية بيع بعميل حالي أو اختيار "عميل عابر" للمبيعات المجهولة.
    -   **إتمام البيع:** حوار دفع كامل يدير طرق دفع متعددة ومعاملات ذرية تضمن سلامة البيانات.
    -   **طباعة الإيصالات:** يولد إيصالًا مفصلاً في نهاية البيع (حراري 80 مم أو A4).

#### التصميم التقني
-   **إدارة الحالة:** يستخدم الخطاف المخصص `useCarts` لإدارة حالة سلات التسوق المتعددة. حالة السلات يتم حفظها في جدول `carts` في IndexedDB لضمان استمراريتها حتى بعد إعادة تحميل الصفحة.
-   **جلب البيانات:** يتم جلب بيانات العملاء والمنتجات بشكل تفاعلي باستخدام `useLiveQuery` لضمان تحديث الواجهة تلقائيًا عند أي تغيير.
-   **منطق الأعمال (الأهم):** تتم عملية إتمام البيع (`dataService.addSale`) داخل معاملة ذرية (`db.transaction()`). هذا يضمن أن الإجراءات التالية تتم كوحدة واحدة لا تتجزأ:
    1.  إنشاء سجل بيع جديد (`sales`).
    2.  لكل عنصر مباع، يتم **إنقاص المخزون** في جدول `products`.
    3.  إنشاء سجل تتبع للمخزون (`inventoryLogs`).
    4.  إذا كانت مرتبطة بعميل، يتم **تحديث رصيده المستحق (`outstandingBalance`) وإجمالي إنفاقه (`totalSpent`)**.

### ج. إدارة المنتجات (`/products`)

#### الوصف الوظيفي
مركز التحكم الكامل لجميع المخزون.

-   **الهدف:** إدارة كتالوج المنتجات، وتتبع مستويات المخزون والأسعار.
-   **الميزات:**
    -   **بحث وتصفية:** أدوات قوية للبحث بالاسم/الباركود والتصفية حسب الفئة، المورد، أو حالة المخزون.
    -   **فرز متقدم:** فرز المنتجات حسب الاسم، السعر، الكمية، أو تاريخ الإضافة.
    -   **أوضاع العرض:** التبديل بين عرض الشبكة (Grid) والقائمة (Table).
    -   **إجراءات جماعية:** تحديد منتجات متعددة لحذفها أو لطباعة ملصقات الباركود الخاصة بها.
    -   **استيراد/تصدير CSV:** استيراد قائمة منتجات من ملف CSV أو تصدير المخزون الحالي.

#### التصميم التقني
-   **جلب البيانات:** تستخدم الصفحة `useLiveQuery` لجلب قائمة المنتجات بشكل تفاعلي. يتم تطبيق الفلاتر والبحث والفرز مباشرة على مستوى استعلام Dexie لضمان أداء عالٍ.
-   **إدارة الحالة:** تستخدم مكونات React `useState` لإدارة حالة الواجهة مثل وضع العرض والمنتجات المحددة.
-   **منطق الأعمال:** تتم عمليات الإضافة والتعديل والحذف عبر `data-service.ts`، مع تحديث الواجهة تلقائيًا بفضل `useLiveQuery`.

### د. إدارة العملاء (`/customers`)

#### الوصف الوظيفي
نظام CRM مصغر لإدارة قاعدة بيانات العملاء وسجلهم.

-   **الهدف:** مركزية معلومات العملاء، تتبع سجل مشترياتهم، وإدارة ديونهم.
-   **الميزات:**
    -   **قائمة العملاء:** تعرض جميع العملاء مع إحصائيات رئيسية مثل الرصيد المستحق وحالة الديون.
    -   **صفحة تفاصيل العميل (`/customers/[id]`):** تعرض سجل نشاط مفصل لجميع تفاعلات العميل.
    -   **إجراءات:** تسجيل دفعة جديدة للعميل أو طباعة كشف حساب مفصل لفواتيره غير المدفوعة.

#### التصميم التقني
-   **جلب البيانات:** تستخدم `useLiveQuery` لعرض قائمة العملاء. صفحة التفاصيل (`/customers/[id]`) تجلب بيانات العميل المحدد وسجل نشاطه الكامل باستخدام دوال مخصصة في `data-service`.
-   **منطق الأعمال:** يتم حساب حالة الدين (`debtStatus`) ديناميكيًا في `dataService.getCustomers` بناءً على تاريخ آخر نشاط وفترة السماح المحددة للعميل.

### هـ. إدارة المخزون (`/stock` & `/costing`)

#### الوصف الوظيفي
وحدات لإدارة المخزون من المنبع (الاستلام والتكاليف).

-   **الهدف:** تسجيل إدخالات البضائع وحساب تكلفتها الحقيقية.
-   **الميزات:**
    -   **استلام المخزون (`/stock/intake`):** تسجيل تسليم جديد، إضافة منتجات موجودة أو إنشاء منتجات جديدة على الفور.
    -   **حساب التكاليف (`/costing`):** أداة لتوزيع التكاليف الإضافية (مثل النقل) على كل عنصر في شحنة لحساب التكلفة النهائية للوحدة، مما يضمن حساب أرباح أكثر دقة.

#### التصميم التقني
-   **منطق الأعمال:** عملية حفظ استلام المخزون (`addStockIntake`) هي معاملة ذرية تضمن التحديث المتسق للمخزون وإنشاء سجلات الموردين والمنتجات.
-   **الحسابات:** تستخدم صفحة حساب التكاليف الخطاف `useMemo` لإعادة حساب التكاليف ديناميكيًا عند تغيير المدخلات.

### و. إدارة طلبات الخبز (`/bread`)

#### الوصف الوظيفي
وحدة متخصصة وقوية لإدارة طلبات الخبز اليومية.

-   **الهدف:** أتمتة وتبسيط إدارة عملاء وطلبات الخبز.
-   **الميزات:**
    -   **إدارة عملاء الخبز:** إضافة وإدارة العملاء بمنطق طلبات متكررة.
    -   **العرض اليومي:** واجهة رئيسية تعرض طلبات يوم واحد، مع تنقل سهل بين الأيام.
    -   **إنشاء تلقائي:** يقوم النظام تلقائيًا بإنشاء طلبات اليوم للعملاء النشطين.
    -   **تحويل إلى مبيعات:** تحديد عدة طلبات لتحويلها إلى فواتير بيع بنقرة واحدة.

#### التصميم التقني
-   **جداول مخصصة:** تستخدم جدولي `clients_pain` و `commandes_pain` لفصل هذا المنطق.
-   **جلب البيانات:** تستخدم الصفحة `useLiveQuery` لجلب الطلبات بشكل تفاعلي.
-   **منطق الأعمال:** تتم عملية إنشاء الطلبات تلقائيًا عبر `useEffect` عند عرض يوم جديد. تتم عملية التحويل إلى مبيعات (`convertBreadOrdersToSales`) كمعاملة ذرية واحدة تضمن إنشاء البيع، تحديث المخزون، وتحديث حالة الطلب.

### ز. الملف الشخصي والإعدادات (`/profile`)

#### الوصف الوظيفي
صفحة التكوين الشاملة للتطبيق.

-   **الهدف:** إدارة معلومات الشركة، الإعدادات، وبيانات التطبيق.
-   **الميزات:**
    -   **ملف الشركة:** تعديل المعلومات التي ستظهر على الإيصالات.
    -   **النسخ الاحتياطي والاستعادة:** تنزيل نسخة احتياطية كاملة من جميع بيانات التطبيق أو استعادتها من ملف JSON.
    -   **منطقة الخطر:** خيار لإعادة تعيين التطبيق بالكامل وحذف جميع البيانات.

#### التصميم التقني
-   **منطق البيانات:** تستخدم دوال `exportData` و `restoreTables` في `data-service.ts` لقراءة جميع جداول IndexedDB وتحويلها إلى JSON أو العكس، مع توفير واجهة معاينة للمستخدم قبل الاستعادة.
-   **واجهة المستخدم:** تستخدم نظام `Tabs` من ShadCN لتقسيم الإعدادات إلى أقسام منطقية.
