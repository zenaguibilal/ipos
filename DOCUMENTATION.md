# iPOS - وصف كامل للتطبيق وبنيته التقنية

يقدم هذا المستند وصفًا تفصيليًا وشاملاً لكل صفحة وميزة رئيسية في تطبيق iPOS، مع تسليط الضوء على طريقة التصميم والجوانب التقنية التي تضمن قوته وموثوقيته، خصوصًا بعد الاعتماد الكامل على Dexie.js.

---
## 1. لوحة التحكم (`/dashboard`)

### الوصف الوظيفي
هذه هي الصفحة الرئيسية والمركز العصبي للتطبيق، حيث توفر نظرة عامة في الوقت الفعلي على أداء العمل.

-   **الهدف:** توفير مؤشرات الأداء الرئيسية (KPIs) ونظرة سريعة على النشاط التجاري.
-   **الميزات:**
    -   **محدد الفترة الزمنية:** مرشح تاريخ قوي لتحليل البيانات عبر فترات مخصصة.
    -   **بطاقات الإحصائيات:** تعرض الأرقام الأساسية: إجمالي الإيرادات، صافي الربح، عدد المبيعات، إجمالي المصروفات، والقيمة الإجمالية للمخزون.
    -   **تنبيهات انخفاض المخزون:** تسلط الضوء على المنتجات التي وصلت إلى حدها الأدنى.
    -   **مخطط الإيرادات والربح:** رسم بياني شريطي مرئي يقارن الإيرادات وصافي الأرباح يوميًا.
    -   **أفضل المنتجات والعملاء:** قوائم للمنتجات والعملاء الأعلى أداءً.
    -   **النشاط الأخير:** عرض لآخر 10 عمليات مسجلة في النظام (بيع، شراء، عميل جديد، إلخ).

### التصميم التقني
-   **جلب البيانات:** تستخدم الصفحة الخطاف المخصص `useDashboardData` لجلب جميع بياناتها بشكل تفاعلي. هذا الخطاف بدوره يستخدم `useLiveQuery` من `dexie-react-hooks` لتجميع البيانات من جداول متعددة (`sales`, `products`, `expenses`) بناءً على النطاق الزمني المحدد، مما يضمن تحديث لوحة التحكم في الوقت الفعلي.
-   **إدارة الحالة:** يتم التحكم في النطاق الزمني بواسطة الخطاف المخصص `useDateRange` الذي يدير حالة فترة العرض.
-   **العرض:** تستخدم مكونات من مكتبة `Recharts` لعرض المخططات البيانية، ومكونات واجهة مستخدم مخصصة (`KPICard`, `StockAlertsCard`, `RecentActivityCard`) لعرض الإحصائيات والبيانات الأخرى.

---
## 2. نقطة البيع (`/sell`)

### الوصف الوظيفي
واجهة الكاشير الرئيسية، مصممة لتكون سريعة وبديهية وفعالة.

-   **الهدف:** تمكين تسجيل المبيعات السريع وإدارة المعاملات.
-   **الميزات:**
    -   **إدارة سلات متعددة:** إمكانية إدارة عدة سلات تسوق في وقت واحد عبر علامات تبويب.
    -   **بحث المنتجات:** حقل بحث قوي للعثور على المنتجات بالاسم أو مسحها ضوئيًا بالباركود.
    -   **اختيار العميل:** ربط عملية بيع بعميل حالي أو اختيار "عميل عابر".
    -   **إتمام البيع:** حوار دفع كامل يدير طرق دفع متعددة ومعاملات ذرية.
    -   **طباعة الإيصالات:** يولد إيصالًا مفصلاً في نهاية البيع (حراري 80 مم أو A4).

### التصميم التقني
-   **إدارة الحالة:** يستخدم الخطاف المخصص `useCarts` لإدارة حالة سلات التسوق المتعددة. حالة السلات يتم حفظها في جدول `carts` في IndexedDB لضمان استمراريتها حتى بعد إعادة تحميل الصفحة، مما يوفر تجربة مستخدم سلسة.
-   **جلب البيانات:** يتم جلب بيانات العملاء والمنتجات بشكل تفاعلي باستخدام `useLiveQuery` لضمان تحديث الواجهة تلقائيًا عند أي تغيير.
-   **منطق الأعمال:** تتم عملية إتمام البيع (`dataService.addSale`) داخل معاملة ذرية (`atomic transaction`) باستخدام `Dexie.transaction()`. هذا يضمن أن تحديث المخزون، وإنشاء سجل البيع، وتحديث رصيد العميل تتم كلها كوحدة واحدة متكاملة، مما يمنع أي تناقض في البيانات.

---
## 3. إدارة المنتجات (`/products`)

### الوصف الوظيفي
مركز التحكم الكامل لجميع المخزون.

-   **الهدف:** إدارة كتالوج المنتجات، وتتبع مستويات المخزون والأسعار.
-   **الميزات:**
    -   **بحث وتصفية:** أدوات قوية للبحث بالاسم/الباركود والتصفية حسب الفئة، المورد، أو حالة المخزون.
    -   **أوضاع العرض:** التبديل بين عرض الشبكة (Grid) والقائمة (Table).
    -   **إجراءات جماعية:** تحديد منتجات متعددة لحذفها دفعة واحدة أو لطباعة ملصقات الباركود الخاصة بها.
    -   **استيراد/تصدير CSV:** استيراد قائمة منتجات من ملف CSV أو تصدير المخزون الحالي.

### التصميم التقني
-   **جلب البيانات:** تستخدم الصفحة `useLiveQuery` لجلب قائمة المنتجات بشكل تفاعلي. يتم تطبيق الفلاتر، البحث، والفرز في الوقت الفعلي على مستوى استعلام Dexie لضمان أداء عالٍ حتى مع وجود عدد كبير من المنتجات.
-   **إدارة الحالة:** يتم استخدام `useState` لإدارة حالة الواجهة مثل وضع العرض، والمنتجات المحددة، وحالة مربعات الحوار.
-   **منطق الأعمال:** تتم عمليات الإضافة والتعديل والحذف عبر `data-service.ts` الذي يتفاعل مباشرة مع Dexie، مع ضمان تحديث الواجهة تلقائيًا بفضل `useLiveQuery`.

---
## 4. إدارة العملاء (`/customers`)

### الوصف الوظيفي
نظام CRM مصغر لإدارة قاعدة بيانات العملاء وسجلهم.

-   **الهدف:** مركزية معلومات العملاء، تتبع سجل مشترياتهم، وإدارة ديونهم.
-   **الميزات:**
    -   **قائمة العملاء:** تعرض جميع العملاء مع إحصائيات رئيسية مثل الرصيد المستحق وحالة الديون.
    -   **صفحة تفاصيل العميل (`/customers/[id]`):** تعرض سجل نشاط مفصل لجميع تفاعلات العميل.
    -   **إجراءات:** تسجيل دفعة جديدة للعميل أو طباعة كشف حساب مفصل لفواتيره غير المدفوعة.

### التصميم التقني
-   **جلب البيانات:** تستخدم `useLiveQuery` لعرض قائمة العملاء مع تطبيق الفلاتر. صفحة التفاصيل (`/customers/[id]`) تجلب بيانات العميل المحدد وسجل نشاطه الكامل من `data-service`.
-   **منطق الأعمال:** يتم تحديث أرصدة العملاء بشكل ذري عند كل عملية بيع، دفع، أو إرجاع لضمان دقة البيانات المالية. يتم حساب حالة الدين (`debtStatus`) ديناميكيًا في `dataService.getCustomers`.

---
## 5. سجل المبيعات (`/sales-history`)

### الوصف الوظيفي
سجل لجميع معاملات البيع السابقة.

-   **الهدف:** توفير وصول سهل إلى سجل المبيعات للتشاور أو الإدارة.
-   **الميزات:**
    -   عرض جميع المبيعات مع البحث والتصفية حسب الفترة الزمنية.
    -   **تفاصيل البيع:** فتح نافذة تعرض تفاصيل عملية البيع.
    -   **إلغاء البيع:** إلغاء عملية بيع، وهو إجراء ذري يعيد المنتجات إلى المخزون ويعدل رصيد العميل.

### التصميم التقني
-   **جلب البيانات:** تستخدم `useLiveQuery` مع `useDateRange` للبحث والتصفية.
-   **منطق الأعمال:** منطق إلغاء البيع (`dataService.deleteSale`) في `data-service.ts` هو معاملة ذرية تضمن عكس جميع التغييرات التي تمت أثناء البيع الأصلي (إعادة المخزون، عكس تأثير الدين).

---
## 6. إدارة المخزون (`/stock` & `/costing`)

### الوصف الوظيفي
وحدات لإدارة المخزون من المنبع (الاستلام والتكاليف).

-   **الهدف:** تسجيل إدخالات البضائع وحساب تكلفتها الحقيقية.
-   **الميزات:**
    -   **استلام المخزون (`/stock/intake`):** تسجيل تسليم جديد، إضافة منتجات موجودة أو إنشاء منتجات جديدة.
    -   **حساب التكاليف (`/costing`):** أداة لتوزيع التكاليف الإضافية (مثل النقل) على كل عنصر في شحنة لحساب التكلفة النهائية للوحدة.

### التصميم التقني
-   **منطق الأعمال:** عملية حفظ استلام المخزون (`addStockIntake`) هي معاملة ذرية تضمن التحديث المتسق للمخزون وإنشاء سجلات الموردين والمنتجات.
-   **الحسابات:** تستخدم صفحة حساب التكاليف الخطاف `useMemo` لإعادة حساب التكاليف ديناميكيًا عند تغيير المدخلات.

---
## 7. إدارة طلبات الخبز (`/bread`)

### الوصف الوظيفي
وحدة متخصصة وقوية لإدارة طلبات الخبز اليومية.

-   **الهدف:** أتمتة وتبسيط إدارة عملاء وطلبات الخبز.
-   **الميزات:**
    -   **إدارة عملاء الخبز:** إضافة وإدارة العملاء بمنطق طلبات متكررة.
    -   **العرض اليومي:** واجهة رئيسية تعرض طلبات يوم واحد، مع تنقل سهل بين الأيام.
    -   **إنشاء تلقائي:** يقوم النظام تلقائيًا بإنشاء طلبات اليوم للعملاء النشطين.
    -   **تحويل إلى مبيعات:** تحديد عدة طلبات لتحويلها إلى فواتير بيع بنقرة واحدة.

### التصميم التقني
-   **جداول مخصصة:** تستخدم جدولي `clients_pain` و `commandes_pain` لفصل هذا المنطق.
-   **جلب البيانات:** تستخدم الصفحة `useLiveQuery` لجلب الطلبات بشكل تفاعلي.
-   **منطق الأعمال:** تتم عملية إنشاء الطلبات تلقائيًا عبر `useEffect` عند عرض يوم جديد. تتم عملية التحويل إلى مبيعات (`convertBreadOrdersToSales`) كمعاملة ذرية واحدة تضمن إنشاء البيع، تحديث المخزون، وتحديث حالة الطلب.

---
## 8. الملف الشخصي والإعدادات (`/profile`)

### الوصف الوظيفي
صفحة التكوين الشاملة للتطبيق.

-   **الهدف:** إدارة معلومات الشركة، الإعدادات، وبيانات التطبيق.
-   **الميزات:**
    -   **ملف الشركة:** تعديل المعلومات التي ستظهر على الإيصالات.
    -   **النسخ الاحتياطي والاستعادة:** تنزيل نسخة احتياطية كاملة من جميع بيانات التطبيق أو استعادتها من ملف JSON.
    -   **منطقة الخطر:** خيار لإعادة تعيين التطبيق بالكامل وحذف جميع البيانات.

### التصميم التقني
-   **منطق البيانات:** تستخدم دوال `exportData` و `restoreTables` في `data-service.ts` لقراءة جميع جداول IndexedDB وتحويلها إلى JSON أو العكس، مع توفير واجهة معاينة للمستخدم قبل الاستعادة.
-   **واجهة المستخدم:** تستخدم نظام `Tabs` من ShadCN لتقسيم الإعدادات إلى أقسام منطقية (ملف شخصي، بيانات، إلخ).
