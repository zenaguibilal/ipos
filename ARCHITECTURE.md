# iPOS - بنية تحتية تعمل 100% بدون اتصال بالإنترنت

يقدم هذا المستند نظرة تقنية معمقة على البنية التحتية لتطبيق iPOS، مع التركيز على تدفق البيانات، القرارات التقنية، والمنطق الحساس للتطبيق بعد إعادة هيكلته ليعتمد بشكل كامل على **Dexie.js**.

## 1. نظرة عامة على التكنولوجيا

iPOS مبني على حزمة تقنية حديثة، تم اختيارها لأدائها، موثوقيتها، وتجربة التطوير.

-   **إطار العمل الرئيسي:** [Next.js (App Router)](https://nextjs.org/) - إطار عمل React يُستخدم لبنيته القائمة على المكونات والتوجيه المستند إلى الملفات.
-   **تخزين البيانات:** **[Dexie.js](https://dexie.org/)** - مكتبة قوية تعمل كطبقة تجريدية فوق IndexedDB. يتم تخزين جميع بيانات التطبيق في قاعدة بيانات منظمة على جهاز المستخدم، مما يضمن استمرارية قوية للبيانات، استعلامات معقدة وعالية الأداء، وتفاعلات آمنة من خلال المعاملات الذرية (atomic transactions).
-   **طبقة الوصول إلى البيانات:**
    -   **`database.ts`** (`src/lib/database.ts`): هذا الملف هو قلب إدارة قاعدة البيانات. يُعرّف مخطط (schema) قاعدة البيانات `posDB`، بما في ذلك جميع الجداول (`products`, `sales`, إلخ) والفهارس الخاصة بها. كما يوفر دالة `getDb()` الآمنة للاستخدام من جانب الخادم (SSR-safe) للحصول على نسخة فريدة (singleton) من قاعدة البيانات.
    -   **`data-service.ts`** (`src/services/data-service.ts`): هذه الخدمة هي الواجهة الوحيدة للتعامل مع منطق الأعمال وقاعدة البيانات. تحتوي على جميع دوال جلب البيانات وتعديلها (مثل `getProducts`, `addSale`). جميع عمليات الكتابة المعقدة (مثل إتمام عملية بيع) يتم تنفيذها داخل معاملات `Dexie.transaction()` لضمان سلامة البيانات.
-   **إدارة الحالة وتحديثات واجهة المستخدم:**
    -   **[dexie-react-hooks](https://dexie.org/docs/dexie-react-hooks/useLiveQuery())**: يتم استخدام الخطاف `useLiveQuery` لجلب البيانات بشكل تفاعلي. أي تغيير في البيانات في قاعدة البيانات يؤدي تلقائيًا إلى إعادة عرض المكونات التي تستخدم هذا الخطاف، مما يضمن واجهة مستخدم محدثة دائمًا وبدون الحاجة إلى إدارة الحالة يدويًا.
-   **واجهة المستخدم (UI):**
    -   **[ShadCN UI](https://ui.shadcn.com/)**: مجموعة من مكونات واجهة المستخدم القابلة لإعادة الاستخدام والمصممة بشكل احترافي.
    -   **[Tailwind CSS](https://tailwindcss.com/)**: إطار عمل CSS يركز على الأدوات المساعدة لتصميمات مخصصة سريعة.
-   **PWA (Progressive Web App):** تم تصميم التطبيق ليعمل كتطبيق ويب تقدمي، مما يسمح بتثبيته على سطح المكتب أو الهاتف المحمول وتشغيله بنسبة 100% بدون اتصال بالإنترنت.

---

## 2. هيكل المشروع

تم تنظيم هيكل الملفات لفصل المسؤوليات بوضوح.

```
/
├── public/                 # الملفات الثابتة (أيقونات، manifest.json)
├── src/
│   ├── app/                # مسارات التطبيق (Next.js App Router)
│   │   ├── (app)/          # تخطيط ومسارات التطبيق الرئيسية
│   │   └── layout.tsx      # التخطيط الجذري
│   │
│   ├── components/         # مكونات React القابلة لإعادة الاستخدام
│   │   ├── ui/             # مكونات واجهة المستخدم الأساسية (Button, Card)
│   │   └── ... (مكونات خاصة بالميزات)
│   │
│   ├── hooks/              # خطافات React المخصصة (useCarts, useDebounce, إلخ)
│   │
│   ├── lib/                # دوال مساعدة، أنواع، وتكوين DB
│   │   ├── database.ts     # تعريف مخطط Dexie ودالة getDb()
│   │   └── types.ts        # تعريفات TypeScript لهياكل البيانات
│   │
│   ├── services/           # خدمات التطبيق الرئيسية
│   │   └── data-service.ts # جوهر منطق استمرارية البيانات (CRUD + Business Logic)
│
└── ... (ملفات التكوين)
```

---

## 3. إدارة البيانات (Dexie.js)

هذا هو جوهر البنية التحتية التي تعمل بدون اتصال بالإنترنت.

1.  **`database.ts` (`src/lib/database.ts`):**
    -   **مصدر الحقيقة الوحيد:** هذا الملف يدير اتصال قاعدة البيانات `posDB` عبر فئة `PosDatabase` التي ترث من `Dexie`.
    -   **المخطط والفهارس:** يقوم بتعريف جميع الجداول (`products`, `customers`, إلخ) وفهارسها باستخدام `this.version().stores()`. الفهارس (مثل `[category+name]` للمنتجات) ضرورية لتمكين استعلامات سريعة وفعالة.
    -   **الترقيات (Upgrades):** يحتوي على منطق لترحيل البيانات من إصدارات قاعدة البيانات القديمة إلى الجديدة، مما يضمن عدم فقدان بيانات المستخدم عند تحديث المخطط.
    -   **`getDb()` الآمنة:** توفر دالة `getDb` وصولاً آمنًا إلى قاعدة البيانات، وتطلق خطأً صريحًا إذا تم استدعاؤها عن طريق الخطأ في بيئة الخادم (SSR)، مما يمنع أخطاء البناء والتشغيل.

2.  **`useLiveQuery` لقراءة البيانات:**
    -   تستخدم جميع مكونات التطبيق الخطاف `useLiveQuery` من `dexie-react-hooks` لجلب البيانات.
    -   **مثال:** `const products = useLiveQuery(() => dataService.getProducts({ query }), [query]);`
    -   هذا الخطاف يراقب الجداول المحددة ويعيد تشغيل الدالة تلقائيًا عند حدوث أي تغيير (إضافة، تحديث، حذف)، مما يؤدي إلى تحديث واجهة المستخدم في الوقت الفعلي وبشكل فعال للغاية.

3.  **`data-service.ts` لكتابة البيانات:**
    -   للحفاظ على فصل واضح للمسؤوليات، يتم تركيز جميع عمليات الكتابة (`add`, `update`, `delete`) ومنطق الأعمال المعقد في `data-service`.
    -   تستدعي المكونات هذه الدوال لتعديل البيانات (على سبيل المثال، `dataService.addProduct(newProduct)`).
    -   **المعاملات الذرية:** جميع العمليات الحساسة التي تتضمن تحديث جداول متعددة (مثل إتمام عملية بيع) يتم تغليفها داخل `db.transaction()`. هذا يضمن أنه إما أن تنجح جميع العمليات معًا أو تفشل جميعها معًا، مما يمنع تمامًا أي تناقض في البيانات.

---

## 4. منطق الميزات الرئيسية (تصميم تقني)

### أ. نقطة البيع (Checkout) (`/sell`)

-   **إدارة الحالة (`useCarts.ts`):** يدير الخطاف `useCarts` حالة سلات التسوق المتعددة. يتم حفظ بيانات السلات في جدول `carts` في IndexedDB لضمان استمراريتها حتى بعد إعادة تحميل الصفحة.
-   **إتمام عملية البيع (`dataService.addSale`):**
    -   تُعد هذه الدالة العملية الأكثر أهمية وحساسية في التطبيق.
    -   تستخدم `db.transaction()` لتنفيذ سلسلة من الإجراءات بشكل ذري (كوحدة واحدة):
        1.  إنشاء سجل بيع جديد في جدول `sales` مع رقم فاتورة فريد.
        2.  لكل عنصر مباع، يتم **إنقاص المخزون** في جدول `products`.
        3.  إنشاء سجل تتبع للمخزون (`inventoryLogs`) لكل عنصر، مع تحديد السبب "sale".
        4.  إذا كانت Vente مرتبطة بعميل، يتم **تحديث رصيده المستحق (`outstandingBalance`) وإجمالي إنفاقه (`totalSpent`)** في جدول `customers`.
    -   يضمن استخدام المعاملة الذرية أنه من المستحيل بيع منتج دون تقليل مخزونه، أو تحديث رصيد العميل بشكل غير صحيح.

### ب. إدارة المرتجعات (`/returns`)

-   **عملية الإرجاع (`dataService.addReturn`):**
    -   تستخدم أيضًا `db.transaction()` لضمان الاتساق.
    -   تنشئ سجل `ProductReturn`.
    -   إذا تم تحديد عنصر لإعادة تخزينه، يتم **زيادة الكمية** في جدول `products`.
    -   تنشئ سجل مخزون (`inventoryLogs`) بالسبب "return".
    -   تعدل رصيد العميل (`outstandingBalance`) بناءً على قيمة الإرجاع والمبلغ المسترد.
-   **إلغاء الإرجاع (`dataService.deleteReturn`):** يقوم بتنفيذ معاملة عكسية لاستعادة الحالة السابقة للمخزون ورصيد العميل.

### ج. استلام المخزون (`/stock/intake`)

-   **معاملة حفظ ذرية (`dataService.addStockIntake`):**
    -   تستخدم `db.transaction()` لضمان اتساق البيانات عند استلام شحنة جديدة.
    -   داخل المعاملة:
        1.  تنشئ أو تسترد سجل `suppliers`.
        2.  تنشئ سجل `StockIntake` للتاريخ.
        3.  لكل عنصر **جديد**، يتم إنشاء سجل جديد في جدول `products`.
        4.  لكل عنصر، يتم **تحديث الكمية (`quantity`) وسعر الشراء (`purchasePrice`)** في `products`.
        5.  يتم إضافة سجل في `inventoryLogs` لكل منتج، لتتبع زيادة المخزون بالسبب `stock_intake`.

### د. إدارة طلبات الخبز (`/bread`)

-   **جداول مخصصة:** تستخدم جدولي `clients_pain` و `commandes_pain` لفصل هذا المنطق.
-   **إنشاء تلقائي (`createDayOrders`):** تعمل هذه الدالة عند تحميل الصفحة ليوم معين. تتحقق من العملاء النشطين وقواعد التكرار الخاصة بهم لإنشاء `BreadOrder` اليوم تلقائيًا إذا لم تكن موجودة. تم تحسين هذا المنطق لتجنب الحلقات اللانهائية.
-   **تحويل إلى مبيعات (`dataService.convertBreadOrdersToSales`):**
    -   هذه معاملة ذرية تأخذ مصفوفة من معرفات `BreadOrder`.
    -   لكل طلب، تنشئ `Sale` جديدة وتحدث مخزون منتج "Pain".
    -   تحدد `BreadOrder` على أنها مدفوعة (`est_paye: true`) وتربطها بمعرف البيع الجديد.

### هـ. النسخ الاحتياطي والاستعادة (`/profile`)

-   **النسخ الاحتياطي (`dataService.exportData`):** تسترد جميع البيانات من جميع جداول Dexie، وتحولها إلى سلسلة JSON، وتطلق تنزيل ملف.
-   **الاستعادة (`dataService.restoreTables`):** تقرأ ملف JSON، تسمح للمستخدم بمعاينة واختيار الجداول، ثم تستخدم `db.table().clear()` و `db.table().bulkPut()` لإفراغ الجداول المستهدفة وإدراج البيانات الجديدة بشكل جماعي وفعال.

توفر هذه البنية القائمة على Dexie تجربة قوية وسريعة وتعمل بالكامل بدون اتصال بالإنترنت، مع ضمان أقصى درجات سلامة البيانات.
