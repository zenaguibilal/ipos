# iPOS - بنية تحتية تعمل 100% بدون اتصال بالإنترنت

يقدم هذا المستند نظرة مفصلة على البنية التحتية لتطبيق iPOS التي تعمل بالكامل بدون اتصال بالإنترنت. تم تصميمه للمساعدة في فهم هيكل المشروع، وتدفق البيانات، والقرارات التقنية الرئيسية بعد إعادة هيكلته ليعتمد على IndexedDB الأصلي.

## 1. نظرة عامة على التكنولوجيا

iPOS مبني على حزمة تقنية حديثة، تم اختيارها لأدائها وتجربة التطوير.

-   **إطار العمل الرئيسي:** [Next.js (App Router)](https://nextjs.org/) - إطار عمل React يُستخدم لبنيته القائمة على المكونات والتوجيه المستند إلى الملفات.
-   **تخزين البيانات:** **IndexedDB الخاص بالمتصفح** - يتم تخزين جميع بيانات التطبيق (المنتجات، المبيعات، العملاء، إلخ) في قاعدة بيانات IndexedDB منظمة. وهذا يضمن استمرارية قوية للبيانات، واستعلامات معقدة، وأداءً عاليًا، وكل ذلك محليًا على جهاز المستخدم.
-   **طبقة الوصول إلى البيانات:**
    -   **`storage.ts`** (`src/lib/storage.ts`): هذا الملف يوفر واجهة منخفضة المستوى للتفاعل مع IndexedDB، مما يبسط عمليات الفتح والوصول إلى `ObjectStore`.
    -   **`data-service.ts`** (`src/services/data-service.ts`): خدمة توفر دوال غير متزامنة بسيطة (`save`، `getAll`، `update`، إلخ) لتجريد التفاعلات مع قاعدة البيانات. جميع منطق الأعمال المعقد والمعاملات (transactions) موجودة هنا.
-   **إدارة الحالة وتحديثات واجهة المستخدم:** يتم تحقيق الاستجابة باستخدام خطافات React القياسية **`useState`** و **`useEffect`**. تقوم المكونات بجلب البيانات بشكل غير متزامن وتحديث حالتها، مع إعادة التحميل اليدوي بعد كل عملية تعديل لضمان بقاء الواجهة محدثة.
-   **واجهة المستخدم (UI):**
    -   **[ShadCN UI](https://ui.shadcn.com/)**: مجموعة من مكونات واجهة المستخدم القابلة لإعادة الاستخدام.
    -   **[Tailwind CSS](https://tailwindcss.com/)**: إطار عمل CSS يركز على الأدوات المساعدة لتصميمات مخصصة سريعة.
-   **النشر و PWA:** مصمم للنشر على منصات الاستضافة الثابتة (مثل Vercel أو Netlify) ومهيأ كتطبيق ويب تقدمي (PWA) للتثبيت على سطح المكتب/الهاتف المحمول وتشغيله بنسبة 100% بدون اتصال بالإنترنت.

---

## 2. هيكل المشروع

تم تنظيم هيكل الملفات لفصل المسؤوليات بوضوح.

```
/
├── public/                 # الملفات الثابتة (أيقونات، manifest.json)
├── src/
│   ├── app/                # مسارات التطبيق (Next.js App Router)
│   │   ├── dashboard/
│   │   ├── products/
│   │   ├── sell/
│   │   ├── ... (مسارات أخرى)
│   │   └── layout.tsx      # التخطيط الجذري للتطبيق
│   │
│   ├── components/         # مكونات React القابلة لإعادة الاستخدام
│   │   ├── ui/             # مكونات واجهة المستخدم الأساسية (Button, Card, إلخ)
│   │   └── ... (مكونات خاصة بالميزات)
│   │
│   ├── hooks/              # خطافات React المخصصة
│   │   └── useCarts.ts     # منطق إدارة سلات التسوق المتعددة (يستخدم React state)
│   │
│   ├── lib/                # دوال مساعدة، أنواع، وتكوين DB
│   │   ├── storage.ts      # واجهة IndexedDB الأصلية
│   │   └── types.ts        # تعريفات TypeScript لهياكل البيانات
│   │
│   ├── services/           # خدمات التطبيق الرئيسية
│   │   └── data-service.ts # جوهر منطق استمرارية البيانات المحلية
│
└── ... (ملفات التكوين)
```

---

## 3. إدارة البيانات (IndexedDB الأصلي)

هذا هو جوهر البنية التحتية التي تعمل بدون اتصال بالإنترنت.

1.  **`storage.ts` (`src/lib/storage.ts`):**
    -   **مصدر الحقيقة الوحيد:** هذا الملف يدير اتصال قاعدة البيانات `iPOS_DB_NATIVE`.
    -   **المخطط والجداول:** يقوم بإنشاء جداول التطبيق (`products`، `customers`، إلخ) وفهارسها عند الحاجة (`onupgradeneeded`).
    -   **الفهرسة:** يتم تحديد الفهارس على الحقول التي يتم الاستعلام عنها بشكل متكرر (مثل `name` للمنتجات، `customerId` للمبيعات) لضمان عمليات بحث وتصفية سريعة وعالية الأداء، حتى مع مجموعات البيانات الكبيرة.

2.  **`useState`/`useEffect` لقراءة البيانات:**
    -   تستخدم مكونات React خطافات `useState` و `useEffect` القياسية لجلب البيانات وإدارتها.
    -   مثال:
        ```jsx
        const [products, setProducts] = useState([]);
        useEffect(() => {
            dataService.getProducts({}).then(setProducts);
        }, []);
        ```
    -   لضمان تحديث واجهة المستخدم، يتم استدعاء دوال إعادة التحميل (`loadProducts`) يدويًا بعد كل عملية تعديل (إضافة، تحديث، حذف) عبر ردود الاتصال (callbacks) التي يتم تمريرها إلى المكونات الفرعية.

3.  **`data-service.ts` لكتابة البيانات:**
    -   للحفاظ على فصل واضح للمخاوف، يتم تركيز جميع عمليات الكتابة (`add`، `update`، `remove`) في `data-service`.
    -   تستدعي المكونات هذه الدوال لتعديل البيانات. على سبيل المثال، `dataService.addProduct(newProduct)`.
    -   تنفذ الخدمة العملية المقابلة على IndexedDB باستخدام دوال المساعدة من `storage.ts`.
    -   بمجرد اكتمال الكتابة، يتم استدعاء دالة رد اتصال لإعادة تحميل البيانات في المكونات المتأثرة وتحديث واجهة المستخدم.

4.  **مثال على تدفق البيانات (صفحة المنتجات):**
    -   يستخدم المكون `ProductsPage` `useEffect` لاستدعاء `dataService.getProducts({})` والحصول على قائمة المنتجات.
    -   عندما يضيف مستخدم منتجًا جديدًا عبر `ProductDialog`، يستدعي الحوار `dataService.addProduct(newProduct)`.
    -   يستخدم `dataService` دالة `db.transaction()` لضمان إضافة المنتج وسجل المخزون بشكل ذري.
    -   بعد إغلاق الحوار، يتم استدعاء دالة `loadProducts` في `ProductsPage`، والتي تعيد جلب البيانات من IndexedDB وتحدث حالة المكون.
    -   يتم إعادة عرض المكون لإظهار المنتج الجديد.

---

## 4. منطق الميزات الرئيسية

### أ. نقطة البيع (Checkout) (`/sell`)

-   **حالة سلة التسوق:** يدير الخطاف `useCarts` عدة سلات تسوق باستخدام `React.useState` و IndexedDB. يتم الاحتفاظ بالحالة عند إعادة تحميل الصفحة.
-   **إتمام عملية البيع:**
    -   تعد الدالة `handleFinalizeSale` (عبر `dataService.addSale`) العملية الأكثر أهمية.
    -   تستخدم `db.transaction()`، وهي ميزة قوية في IndexedDB.
    -   داخل كتلة المعاملة، تقوم بتنفيذ عدة إجراءات رئيسية بشكل ذري:
        1.  إنشاء سجل بيع جديد في جدول `sales`.
        2.  لكل عنصر مباع، يتم إنقاص المخزون في جدول `products`.
        3.  إنشاء سجل مخزون (`inventoryLogs`) لكل عنصر.
        4.  تحديث الرصيد المستحق والمبلغ الإجمالي الذي أنفقه العميل في جدول `customers` إذا كان هناك عميل مرتبط.
    -   يضمن استخدام المعاملة أن جميع العمليات تنجح أو تفشل معًا، مما يمنع التناقضات في البيانات (على سبيل المثال، بيع منتج دون تقليل مخزونه).

### ب. إدارة المرتجعات (`/returns`)

-   **عملية الإرجاع:** تسمح بتسجيل مرتجع بناءً على عملية بيع موجودة. يبحث المستخدم عن فاتورة، ويختار العناصر المراد إرجاعها، ويحدد الكمية.
-   **معاملة ذرية (`dataService.addReturn`):**
    -   تنشئ سجل `ProductReturn`.
    -   إذا تم تحديد عنصر لإعادة تخزينه، يتم تحديث الكمية في جدول `products`.
    -   تنشئ سجل مخزون (`inventoryLogs`) بالسبب `return`.
    -   تعدل رصيد العميل (`outstandingBalance`) بناءً على قيمة الإرجاع والمبلغ المسترد.
-   **الإلغاء:** يقوم إلغاء الإرجاع (`dataService.deleteReturn`) بتنفيذ معاملة عكسية لاستعادة الحالة السابقة للمخزون ورصيد العميل.

### ج. استلام المخزون (`/stock/intake`)

هذه الصفحة ضرورية لإدارة المخزون من المنبع.

-   **واجهة الإدخال:** يمكن للمستخدم اختيار مورد موجود أو إنشاء مورد جديد، ثم إضافة منتجات موجودة أو إنشاء عناصر جديدة على الفور.
-   **معاملة حفظ ذرية (`dataService.addStockIntake`):**
    -   تستخدم `db.transaction()` لضمان اتساق البيانات.
    -   داخل المعاملة:
        1.  تنشئ أو تسترد سجل `suppliers`.
        2.  تنشئ سجل `StockIntake` للتاريخ.
        3.  لكل عنصر **جديد**، يتم إنشاء سجل جديد في جدول `products`.
        4.  لكل عنصر، يتم تحديث الكمية (`quantity`) وسعر الشراء (`purchasePrice`) في `products`.
        5.  يتم إضافة سجل في `inventoryLogs` لكل منتج، لتتبع زيادة المخزون بالسبب `stock_intake`.

### د. حساب التكاليف (`/costing`)

أداة التحليل المالي هذه توسع وظائف استلام المخزون.

-   **منطق الحساب:** يقوم النظام بتوزيع التكاليف الإضافية (مثل النقل) على كل عنصر في شحنة، بشكل متناسب مع قيمة شرائه.
-   **تحديث الأسعار:** تقوم دالة `applyNewPurchasePrices` بتحديث حقل `purchasePrice` لكل منتج معني، مما يضمن أن حسابات الأرباح المستقبلية تستند إلى التكلفة الحقيقية للاستحواذ.

### هـ. إدارة طلبات الخبز (`/bread`)

وحدة متخصصة لإدارة طلبات الخبز المتكررة.

-   **جداول مخصصة:** تستخدم جدولي `clients_pain` و `commandes_pain` لفصل هذا المنطق.
-   **إنشاء تلقائي:** تعمل دالة `createDayOrders` عند تحميل الصفحة ليوم معين. تتحقق من العملاء النشطين وقواعد التكرار الخاصة بهم (`quotidien` أو `jours_specifiques`) لإنشاء `BreadOrder` اليوم تلقائيًا إذا لم تكن موجودة.
-   **تحويل إلى مبيعات (`dataService.convertBreadOrdersToSales`):**
    -   هذه معاملة ذرية تأخذ مصفوفة من معرفات `BreadOrder`.
    -   لكل طلب، تنشئ `Sale` جديدة.
    -   تحدث `outstandingBalance` للعميل الرئيسي (إذا كان مرتبطًا) في جدول `customers`.
    -   تحدد `BreadOrder` على أنها مدفوعة (`est_paye: true`) وتربطها بمعرف البيع الجديد (`vente_id`).

### و. النسخ الاحتياطي والاستعادة (`/profile`)

-   **النسخ الاحتياطي:** تسترد دالة `handleBackup` (عبر `dataService.exportData`) جميع البيانات من جميع الجداول، وتحولها إلى سلسلة JSON، وتطلق تنزيل ملف.
-   **الاستعادة:** تقرأ دالة `handleRestore` (عبر `BackupPreview` و `dataService.restoreTables`) ملف JSON، وتسمح للمستخدم بمعاينة واختيار الجداول المراد استعادتها، ثم تفرغ هذه الجداول بالكامل وتدرج البيانات من ملف النسخ الاحتياطي بشكل جماعي.

توفر هذه البنية تجربة قوية وسريعة وتعمل بالكامل بدون اتصال بالإنترنت من خلال التعامل مع قاعدة بيانات IndexedDB في المتصفح كقاعدة بيانات أساسية.
