'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function BakeryPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>صفحة المخبوزات</CardTitle>
      </CardHeader>
      <CardContent>
        <p>تم حذف محتوى هذه الصفحة حسب الطلب.</p>
      </CardContent>
    </Card>
  );
}
