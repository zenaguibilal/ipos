/**
 * @fileOverview OBLITERATED: Browser Supabase Client
 * ❌ DIRECT ACCESS FROM UI IS FORBIDDEN.
 * ❌ ALL DATA MUST FLOW THROUGH THE API WALL.
 * 
 * This file is kept as an empty shell to prevent build errors while refactoring, 
 * but any attempt to import it should be flagged as a critical failure.
 */

export function createClient() {
  throw new Error("ARCHITECTURAL_VIOLATION: Direct Supabase access from browser is forbidden. Use the API Wall.");
}
