// This barrel file is intentionally empty to prevent circular dependencies.
// All modules should be imported directly from their specific file path.
// For example: `import { productRepository } from './product.repository';`
