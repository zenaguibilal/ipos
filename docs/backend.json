{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product sold in the POS system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the product."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the product."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the product image.",
          "format": "uri"
        },
        "price": {
          "type": "number",
          "description": "Selling price of the product in cents."
        },
        "purchasePrice": {
          "type": "number",
          "description": "Purchase price of the product in cents."
        },
        "quantity": {
          "type": "number",
          "description": "Current quantity of the product in stock."
        },
        "minStock": {
          "type": "number",
          "description": "Minimum stock level before an alert is triggered."
        },
        "supplierId": {
          "type": "string",
          "description": "Reference to Supplier. (Relationship: Supplier 1:N Product)"
        },
        "barcode": {
          "type": "string",
          "description": "The barcode value of the product."
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "quantity",
        "supplierId",
        "purchasePrice",
        "minStock"
      ]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer who makes purchases.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Customer entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the customer."
        },
        "phone": {
          "type": "string",
          "description": "Phone number of the customer."
        },
        "debt": {
          "type": "number",
          "description": "The total amount of debt the customer has, in cents."
        },
        "settlementDay": {
          "type": "number",
          "description": "The day of the month the customer settles their debts."
        }
      },
      "required": [
        "id",
        "name",
        "phone"
      ]
    },
    "Supplier": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Supplier",
      "type": "object",
      "description": "Represents a supplier of products.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Supplier entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the supplier."
        },
        "contactName": {
          "type": "string",
          "description": "Contact name at the supplier."
        },
        "contactPhone": {
          "type": "string",
          "description": "Contact phone number at the supplier."
        },
        "contactEmail": {
          "type": "string",
          "description": "Contact email address at the supplier.",
          "format": "email"
        },
        "debt": {
          "type": "number",
          "description": "The total amount of debt owed to the supplier, in cents."
        },
        "visitingDays": {
          "type": "string",
          "description": "The days of the week the supplier visits, as a comma-separated string."
        }
      },
      "required": [
        "id",
        "name",
        "contactName"
      ]
    },
    "Sale": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Sale",
      "type": "object",
      "description": "Represents a sale transaction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Sale entity."
        },
        "invoiceNumber": {
            "type": "number",
            "description": "The sequential invoice number for the sale."
        },
        "customerId": {
          "type": "string",
          "description": "Reference to Customer. (Relationship: Customer 1:N Sale)"
        },
        "saleDate": {
          "type": "string",
          "description": "Date and time of the sale.",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number",
          "description": "Total amount of the sale."
        },
        "paymentMethod": {
          "type": "string",
          "description": "Payment method used for the sale (e.g., cash, card, e-wallet)."
        },
        "saleLineItemIds": {
          "type": "array",
          "description": "References to SaleLineItems. (Relationship: SaleLineItem N:N Sale)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "invoiceNumber",
        "customerId",
        "saleDate",
        "totalAmount",
        "paymentMethod"
      ]
    },
    "SaleLineItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SaleLineItem",
      "type": "object",
      "description": "Represents a line item in a sale transaction.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the SaleLineItem entity."
        },
        "productId": {
          "type": "string",
          "description": "Reference to Product. (Relationship: Product 1:N SaleLineItem)"
        },
        "quantity": {
          "type": "number",
          "description": "Quantity of the product sold in this line item."
        },
        "unitPrice": {
          "type": "number",
          "description": "Price of one unit of the product at the time of the sale."
        },
        "discount": {
          "type": "number",
          "description": "Discount applied to this line item."
        }
      },
      "required": [
        "id",
        "productId",
        "quantity",
        "unitPrice"
      ]
    },
    "StockAlert": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockAlert",
      "type": "object",
      "description": "Represents an alert for low stock levels.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StockAlert entity."
        },
        "productId": {
          "type": "string",
          "description": "Reference to Product. (Relationship: Product 1:N StockAlert)"
        },
        "alertDate": {
          "type": "string",
          "description": "Date and time when the alert was triggered.",
          "format": "date-time"
        },
        "predictedRunOutDate": {
          "type": "string",
          "description": "Predicted date when the stock will run out, based on AI analysis.",
          "format": "date-time"
        },
        "alertSent": {
          "type": "boolean",
          "description": "Indicates whether the alert has been sent to the staff."
        }
      },
      "required": [
        "id",
        "productId",
        "alertDate",
        "predictedRunOutDate",
        "alertSent"
      ]
    },
     "InvoiceCounter": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InvoiceCounter",
      "type": "object",
      "description": "A global counter to ensure unique, sequential invoice numbers.",
      "properties": {
        "id": {
          "type": "string",
          "description": "The ID of the counter document (e.g., 'sales')."
        },
        "lastNumber": {
          "type": "number",
          "description": "The last invoice number that was used."
        }
      },
      "required": ["id", "lastNumber"]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/suppliers/{supplierId}",
        "definition": {
          "entityName": "Supplier",
          "schema": {
            "$ref": "#/backend/entities/Supplier"
          },
          "description": "Stores supplier data. Each document represents a supplier in the system.",
          "params": [
            {
              "name": "supplierId",
              "description": "The unique identifier of the supplier."
            }
          ]
        }
      },
      {
        "path": "/suppliers/{supplierId}/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores products associated with a specific supplier. Uses path-based ownership for security. Includes denormalized 'supplierId' for authorization independence.",
          "params": [
            {
              "name": "supplierId",
              "description": "The unique identifier of the supplier."
            },
            {
              "name": "productId",
              "description": "The unique identifier of the product."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer data. Each document represents a customer.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier of the customer."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}/sales/{saleId}",
        "definition": {
          "entityName": "Sale",
          "schema": {
            "$ref": "#/backend/entities/Sale"
          },
          "description": "Stores sales records associated with a specific customer. Uses path-based ownership for security. Includes denormalized 'customerId' for authorization independence.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier of the customer."
            },
            {
              "name": "saleId",
              "description": "The unique identifier of the sale."
            }
          ]
        }
      },
      {
        "path": "/sales_line_items/{saleLineItemId}",
        "definition": {
          "entityName": "SaleLineItem",
          "schema": {
            "$ref": "#/backend/entities/SaleLineItem"
          },
          "description": "Stores individual line items for sales.",
          "params": [
            {
              "name": "saleLineItemId",
              "description": "The unique identifier of the sale line item."
            }
          ]
        }
      },
      {
        "path": "/stock_alerts/{stockAlertId}",
        "definition": {
          "entityName": "StockAlert",
          "schema": {
            "$ref": "#/backend/entities/StockAlert"
          },
          "description": "Stores stock alerts generated by the AI system.",
          "params": [
            {
              "name": "stockAlertId",
              "description": "The unique identifier of the stock alert."
            }
          ]
        }
      },
      {
        "path": "/counters/{counterId}",
        "definition": {
          "entityName": "InvoiceCounter",
          "schema": {
            "$ref": "#/backend/entities/InvoiceCounter"
          },
          "description": "Global counters for things like invoice numbers.",
          "params": [
            {
              "name": "counterId",
              "description": "The name of the counter (e.g., 'sales')."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure is designed to support the iPOS application's core features: sales, product management, customer management, and supplier management. The structure prioritizes authorization independence and simplifies security rules. Path-based ownership is used for entities associated with specific suppliers and customers. Each collection is designed to have a homogeneous security posture, meaning all documents within a single collection share the same security requirements. This eliminates the need for complex rule logic that distinguishes between different types of documents within the same collection. \n\nAuthorization Independence:\nTo ensure authorization independence and avoid `get()` calls in security rules, the `Supplier` and `Customer` entities will have dedicated collections. The `Product` entities are stored as subcollections of `Supplier` documents, enabling secure and straightforward ownership-based security rules. Sales data is associated with customers via a subcollection, also enforcing ownership.\n\nQAPs Support:\nThe structure uses structural segregation to ensure that each collection has a homogeneous security posture. This, combined with path-based ownership, facilitates secure `list` operations.  Data denormalization (copying relevant supplier/customer data where needed) avoids the need for complex joins or server-side filtering, improving performance and security."
  }
}