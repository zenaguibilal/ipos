{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product sold in the POS system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the product."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the product."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the product image.",
          "format": "uri"
        },
        "price": {
          "type": "number",
          "description": "Selling price of the product in cents."
        },
        "purchasePrice": {
          "type": "number",
          "description": "Purchase price of the product in cents."
        },
        "quantity": {
          "type": "number",
          "description": "Current quantity of the product in stock."
        },
        "minStock": {
          "type": "number",
          "description": "Minimum stock level before an alert is triggered."
        },
        "supplierId": {
          "type": "string",
          "description": "Reference to Supplier. (Relationship: Supplier 1:N Product)"
        },
        "barcode": {
          "type": "string",
          "description": "The barcode value of the product."
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "quantity",
        "supplierId",
        "purchasePrice",
        "minStock"
      ]
    },
    "Customer": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Customer",
      "type": "object",
      "description": "Represents a customer who makes purchases.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Customer entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the customer."
        },
        "phone": {
          "type": "string",
          "description": "Phone number of the customer."
        },
        "debt": {
          "type": "number",
          "description": "The total amount of debt the customer has, in cents."
        },
        "settlementDay": {
          "type": "number",
          "description": "The day of the month the customer settles their debts."
        }
      },
      "required": [
        "id",
        "name",
        "phone"
      ]
    },
    "Supplier": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Supplier",
      "type": "object",
      "description": "Represents a supplier of products.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Supplier entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the supplier."
        },
        "contactName": {
          "type": "string",
          "description": "Contact name at the supplier."
        },
        "contactPhone": {
          "type": "string",
          "description": "Contact phone number at the supplier."
        },
        "debt": {
          "type": "number",
          "description": "The total amount of debt owed to the supplier, in cents."
        },
        "visitingDays": {
          "type": "string",
          "description": "The days of the week the supplier visits, as a comma-separated string."
        }
      },
      "required": [
        "id",
        "name",
        "contactName"
      ]
    },
    "StockAlert": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StockAlert",
      "type": "object",
      "description": "Represents an alert for low stock levels.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StockAlert entity."
        },
        "productId": {
          "type": "string",
          "description": "Reference to Product. (Relationship: Product 1:N StockAlert)"
        },
        "alertDate": {
          "type": "string",
          "description": "Date and time when the alert was triggered.",
          "format": "date-time"
        },
        "predictedRunOutDate": {
          "type": "string",
          "description": "Predicted date when the stock will run out, based on AI analysis.",
          "format": "date-time"
        },
        "alertSent": {
          "type": "boolean",
          "description": "Indicates whether the alert has been sent to the staff."
        }
      },
      "required": [
        "id",
        "productId",
        "alertDate",
        "predictedRunOutDate",
        "alertSent"
      ]
    },
    "BakeryOrder": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "BakeryOrder",
      "type": "object",
      "description": "Represents an order for a bakery item like bread or meloui.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the BakeryOrder entity."
        },
        "customerName": {
          "type": "string",
          "description": "Name of the customer placing the order."
        },
        "quantity": {
          "type": "number",
          "description": "The quantity of the item ordered."
        },
        "type": {
          "type": "string",
          "description": "The type of bakery item.",
          "enum": ["bread", "meloui"]
        },
        "paymentStatus": {
          "type": "string",
          "description": "The payment status of the order.",
          "enum": ["paid", "unpaid"]
        },
        "orderDate": {
          "type": "string",
          "description": "Date and time of the order.",
          "format": "date-time"
        },
        "isFulfilled": {
          "type": "boolean",
          "description": "Whether the order has been fulfilled."
        },
        "isRecurring": {
            "type": "boolean",
            "description": "Whether the order should repeat daily."
        }
      },
      "required": ["id", "customerName", "quantity", "type", "orderDate", "isFulfilled", "paymentStatus"]
    },
    "Sale": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Sale",
      "type": "object",
      "description": "Represents a single sales transaction.",
      "properties": {
        "id": {
          "type": "string"
        },
        "invoiceNumber": {
            "type": "number"
        },
        "customerId": {
          "type": "string"
        },
        "saleDate": {
          "type": "string",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number"
        },
        "paymentMethod": {
          "type": "string",
          "enum": ["cash", "credit"]
        },
        "saleLineItemIds": {
            "type": "array",
            "items": {
                "type": "string"
            }
        }
      },
      "required": ["id", "customerId", "saleDate", "totalAmount", "paymentMethod", "saleLineItemIds"]
    },
    "SaleLineItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SaleLineItem",
      "type": "object",
      "description": "Represents a single line item within a sale.",
      "properties": {
        "id": {
          "type": "string"
        },
        "productId": {
          "type": "string"
        },
        "quantity": {
          "type": "number"
        },
        "unitPrice": {
          "type": "number"
        },
        "discount": {
          "type": "number"
        }
      },
      "required": ["id", "productId", "quantity", "unitPrice"]
    },
    "InvoiceCounter": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "InvoiceCounter",
        "type": "object",
        "description": "A counter to generate sequential invoice numbers.",
        "properties": {
            "id": { "type": "string" },
            "lastNumber": { "type": "number" }
        },
        "required": ["id", "lastNumber"]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/suppliers/{supplierId}",
        "definition": {
          "entityName": "Supplier",
          "schema": {
            "$ref": "#/backend/entities/Supplier"
          },
          "description": "Stores supplier data. Each document represents a supplier in the system.",
          "params": [
            {
              "name": "supplierId",
              "description": "The unique identifier of the supplier."
            }
          ]
        }
      },
      {
        "path": "/suppliers/{supplierId}/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores products associated with a specific supplier. Uses path-based ownership for security. Includes denormalized 'supplierId' for authorization independence.",
          "params": [
            {
              "name": "supplierId",
              "description": "The unique identifier of the supplier."
            },
            {
              "name": "productId",
              "description": "The unique identifier of the product."
            }
          ]
        }
      },
      {
        "path": "/customers/{customerId}",
        "definition": {
          "entityName": "Customer",
          "schema": {
            "$ref": "#/backend/entities/Customer"
          },
          "description": "Stores customer data. Each document represents a customer.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier of the customer."
            }
          ]
        }
      },
       {
        "path": "/customers/{customerId}/sales/{saleId}",
        "definition": {
          "entityName": "Sale",
          "schema": {
            "$ref": "#/backend/entities/Sale"
          },
          "description": "Stores sales for a specific customer.",
          "params": [
            {
              "name": "customerId",
              "description": "The unique identifier of the customer."
            },
            {
                "name": "saleId",
                "description": "The unique identifier of the sale."
            }
          ]
        }
      },
      {
        "path": "/sales_line_items/{lineItemId}",
        "definition": {
            "entityName": "SaleLineItem",
            "schema": { "$ref": "#/backend/entities/SaleLineItem" },
            "description": "Stores individual line items for all sales.",
            "params": [{ "name": "lineItemId", "description": "The unique ID of the line item."}]
        }
      },
      {
        "path": "/stock_alerts/{stockAlertId}",
        "definition": {
          "entityName": "StockAlert",
          "schema": {
            "$ref": "#/backend/entities/StockAlert"
          },
          "description": "Stores stock alerts generated by the AI system.",
          "params": [
            {
              "name": "stockAlertId",
              "description": "The unique identifier of the stock alert."
            }
          ]
        }
      },
       {
        "path": "/bakery_orders/{orderId}",
        "definition": {
          "entityName": "BakeryOrder",
          "schema": {
            "$ref": "#/backend/entities/BakeryOrder"
          },
          "description": "Stores orders for bakery items.",
          "params": [
            {
              "name": "orderId",
              "description": "The unique identifier of the bakery order."
            }
          ]
        }
      },
      {
          "path": "/counters/sales",
          "definition": {
              "entityName": "InvoiceCounter",
              "schema": { "$ref": "#/backend/entities/InvoiceCounter"},
              "description": "Global counters for the application, such as for invoice numbers."
          }
      }
    ],
    "reasoning": "The Firestore data structure is designed to support the iPOS application's core features: product management, customer management, and supplier management. The structure prioritizes authorization independence and simplifies security rules. Path-based ownership is used for entities associated with specific suppliers and customers. Each collection is designed to have a homogeneous security posture, meaning all documents within a single collection share the same security requirements. This eliminates the need for complex rule logic that distinguishes between different types of documents within the same collection. \n\nAuthorization Independence:\nTo ensure authorization independence and avoid `get()` calls in security rules, the `Supplier` and `Customer` entities will have dedicated collections. The `Product` entities are stored as subcollections of `Supplier` documents, enabling secure and straightforward ownership-based security rules. \n\nQAPs Support:\nThe structure uses structural segregation to ensure that each collection has a homogeneous security posture. This, combined with path-based ownership, facilitates secure `list` operations.  Data denormalization (copying relevant supplier/customer data where needed) avoids the need for complex joins or server-side filtering, improving performance and security."
  }
}
    